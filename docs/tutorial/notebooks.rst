Notebooks
=========

.. * ``solver_comparison.ipynb`` — forward FEM solve on a single W7-X coil, comparing ``run()`` timing (compile+run vs warm run) across the ``umfpack``, ``petsc``, and ``cudss`` linear solvers (in ``docs/tutorial/``).
.. * ``optimize_von_mises.ipynb`` — gradient-based optimisation of a von Mises stress objective (in ``docs/tutorial/``).

.. To render notebooks inside Sphinx later, add ``nbsphinx`` or ``myst-nb`` to the doc dependencies and list ``../tutorial/solver_comparison`` in a toctree.
